const DEFAULT_OPTIONS = {
  addSleep: false,
}

const DEFAULT_CLI_OPTIONS = {
  ...DEFAULT_OPTIONS,
  output: 'loadtest.js',
}

const DEFAULT_FUNCTION_NAME = 'main'

module.exports = {
  DEFAULT_OPTIONS,
  DEFAULT_CLI_OPTIONS,
  DEFAULT_FUNCTION_NAME,
}
